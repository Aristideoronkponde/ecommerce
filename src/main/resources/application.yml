# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: ecommerce
  datasource:
    url: jdbc:mysql://localhost:3306/java_ecommerce?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ''
  jpa:
    show-sql: true
    hibernate:
        ddl-auto: update
server:
  port: 8081
